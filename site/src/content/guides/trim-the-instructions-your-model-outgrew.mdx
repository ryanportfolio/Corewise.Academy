---
title: Trim the instructions your model outgrew
description: 'Audit Claude Code instructions, preserve project requirements, and test a smaller setup against the work you already trust.'
track: prompting
level: practitioner
number: 30
minutes: 6
tags: ['instruction files', 'claude code', 'verification']
objectives:
  - Separate fixed project requirements from habits worth testing without.
  - Move task-specific instructions into files loaded when needed.
  - Compare an existing setup with a smaller version using the same task and checks.
prerequisites:
  - slug: stop-shouting-at-the-model
    note: Understand how overly forceful instructions can distort a response.
sources:
  - url: https://www.youtube.com/watch?v=UBFHTHUs1wA
    creator: Duncan Rogoff
    video: Anthropic Engineers Just Fixed Claude Code and Nobody's Talking About it!
    timestamps: ['1:37', '3:44', '5:05', '6:52', '7:32', '8:19', '8:36']
  - url: https://www.youtube.com/watch?v=kCUZqM8-Z-4
    creator: Austin Marchese
    video: The REAL Reason The Creator of Claude Code Told You To Delete Everything
    timestamps: ['0:50', '5:20', '5:45', '7:09', '8:16', '9:09', '10:44', '11:42']
  - url: https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models
    creator: Thariq Shihipar, Anthropic
    video: The new rules of context engineering for Claude 5 generation models
  - url: https://code.claude.com/docs/en/memory
    creator: Anthropic
    video: How Claude remembers your project
  - url: https://code.claude.com/docs/en/cli-reference
    creator: Anthropic
    video: CLI reference
  - url: https://code.claude.com/docs/en/best-practices
    creator: Anthropic
    video: Best practices for Claude Code
selfCheck:
  - q: Which instructions should survive a cleanup even without a recorded failure?
    a: Fixed requirements such as approval before sending, confidentiality limits, and required output formats. A missing failure record does not make a requirement optional.
  - q: Does moving text into a file imported with @path reduce startup context?
    a: No. Claude Code expands imports at startup. Use a task-specific file with a clear instruction about when to read it, then check that the relevant task loads it.
  - q: Why is a safe-mode run an imperfect test of shorter instructions?
    a: Safe mode also disables custom tools, skills, hooks, and memory. A changed result may come from losing those capabilities. Compare the old and revised instructions with the same tools to test the edit itself.
status: published
lastUpdated: 2026-09-15
---

import PullQuote from '../../components/PullQuote.astro';
import Exercise from '../../components/Exercise.astro';

<p class="lede">Keep the instructions that describe your project and test whether the rest still help. Austin Marchese's response to Boris Cherny's advice to delete old customizations adds a useful limit: preserve intentional boundaries. Back up the setup, change one part, and compare the results before keeping the edit.</p>

## <span class="sec-no">01</span>Keep the fixed requirements

Anthropic [reported removing more than 80 percent of Claude Code's system prompt](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models) for Claude 5 models without measurable loss on its coding evaluations. That result concerns Anthropic's tasks. Your approval rules and client requirements need their own assessment.

Duncan Rogoff's walkthrough introduced this guide's pruning method. Marchese's later video supplies the distinction to apply before removing a line:

| Instruction describes | What to do |
| --- | --- |
| A fixed requirement, such as approval before sending a report | Keep it; preserve the permission controls that enforce it |
| A project fact the model cannot infer, such as why a dependency stays pinned | Keep it near the work that needs it |
| A style preference expressed as an absolute, such as never using lists | State the reader's need and when the preference applies |
| A workaround for an old model error | Test the task without that workaround |
| Two conflicting directions | Resolve which applies, then remove or narrow the other |

<PullQuote
  quote="Do not delete intentional boundaries."
  creator="Austin Marchese"
  video="The REAL Reason The Creator of Claude Code Told You To Delete Everything"
  href="https://www.youtube.com/watch?v=kCUZqM8-Z-4"
  timestamp="10:44"
/>

[Stop shouting at the model](/guides/stop-shouting-at-the-model/) covers overly forceful wording. Keep instructions backed by recurring failures, as described in [Turn repeated fixes into rules](/guides/turn-repeated-fixes-into-rules/). For actions with consequences, [Assume your agent uses every tool it has](/guides/assume-your-agent-uses-every-tool-it-has/) explains why written limits need tool restrictions too.

## <span class="sec-no">02</span>Keep examples with a purpose

At 7:09, Marchese suggests replacing rigid examples with a description of the result needed. He also keeps examples for work that must follow an established format. A monthly report may need the same columns because another system imports them.

For an open-ended report, state the decision it should support, the source data, and how claims should be checked. For a fixed report, include its approved template. Label which parts of an example are required and which the model may change.

The following is an original prompt for reviewing instructions, not a prompt transcribed from the video:

```text
Review this report-writing skill without editing it. Separate fixed requirements, project facts, preferences, repeated instructions, and possible outdated workarounds. Cite the file and line for each finding. Preserve required fields and approval boundaries. For each proposed change, name a task and a check that would reveal whether the change made the result worse. Return a proposed diff for review.
```

Use [Brief the model like a brilliant new hire](/guides/brief-the-model/) when the missing information is the task itself. Instruction cleanup cannot supply a goal nobody has stated.

## <span class="sec-no">03</span>Load detail when work needs it

Keep frequently needed project facts in `CLAUDE.md`. Put occasional procedures in a skill or a separate reference file with a clear condition for reading it. [Expertise you can install](/guides/expertise-you-can-install/) covers how those procedures are packaged.

For example, the standing file can say: "Before writing customer-facing copy, read `docs/writing-style.md`." Test a writing request afterward to confirm the agent opens the file.

An `@path` import behaves differently: [Claude Code expands imports at startup](https://code.claude.com/docs/en/memory). Splitting one large instruction file into several imported files organizes the text but still loads it. [Stop hitting your token limit](/guides/stop-hitting-your-token-limit/) covers budgeting that context.

Auto memory is another source of context. Use `/memory` to inspect its settings and saved notes. Keep deliberate project instructions under review even when automatic note-taking is enabled; [Audit what your agent saved to memory](/guides/audit-what-your-agent-saved-to-memory/) covers duplicate and stale notes.

## <span class="sec-no">04</span>Name the check that proves done

At 9:45, Marchese argues that a generic instruction to verify may repeat behavior the model already attempts. His practical recommendation is to specify how this particular output should be verified.

For a report, name the source totals that must match. For code, name the relevant test command and expected result. Anthropic's [best practices](https://code.claude.com/docs/en/best-practices) still recommend giving Claude runnable checks and asking for evidence. An agent saying it verified something leaves the result unproven until you can inspect that evidence.

Keep a check that catches an observed failure. Remove repeated checking only after establishing that both checks cover the same requirement. [Make skills improve after each run](/guides/make-skills-improve-after-each-run/) covers testing those edits against earlier failures.

## <span class="sec-no">05</span>Compare before keeping the edit

Marchese proposes comparing an old setup, a revised setup, and a run without customizations. He later says he usually skips measurement. Our recommendation is to keep a small comparison: a shorter file alone cannot show that the work improved.

Use fresh sessions, the same model, identical input files, and the same completion checks. Record correctness, manual repairs, elapsed time, and cost when available. Repeat a close or inconsistent result before drawing a conclusion. [Compare AI agents on your own work](/guides/compare-ai-agents-on-your-own-work/) develops this method.

The documented terminal command is:

```sh
claude --safe-mode
```

[Safe mode disables customizations](https://code.claude.com/docs/en/cli-reference), including instruction files, skills, hooks, MCP servers, and auto memory. Normal permissions still apply. Because custom hooks and tools disappear too, use a disposable copy and a task that needs no external actions. Treat this run as a configuration diagnostic; it changes more than the instructions.

For the old-versus-revised comparison, keep tools and permission controls unchanged. Anthropic also [recommends `/doctor` for instruction and skill cleanup](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models). Read its findings as candidates to test; the report cannot decide which client requirements still apply.

<Exercise>
1. Save a local backup of the instruction files, skills, and relevant settings, including personal files outside the repository. Check that you can find and restore the originals.
2. Choose one repeated task and write its pass criteria before editing. Run the existing setup and save the output and check results.
3. Propose one narrow change using the review prompt above. Preserve fixed requirements, verification criteria, and permission controls.
4. Run the same task in a fresh session with the revised instructions. Confirm any moved reference file was loaded when needed.
5. Compare both outputs against the original criteria. Keep the edit only if the result still passes and the change has a concrete benefit. Restore it if a required behavior was lost.
</Exercise>
